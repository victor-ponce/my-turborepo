import * as React from "react";
export const Button = () => {
  return <button style={styles.button}>Push me Erick</button>;
};

const styles = {
  button: {
    backgroundColor: "orange",
    fontWeight: "bold"
  }
}
